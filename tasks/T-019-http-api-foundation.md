---
id: T-019
title: HTTP API service foundation — account context, validation, error envelope, authorization seam
stage: validate
owner_agent: builder
status: in_progress
depends_on: [T-014, T-015, T-017, T-018]
file_ownership:
  - "services/api/src/**"
  - "services/api/test/**"
  - "services/api/package.json"   # scripts field ONLY, per ADR-009
spec_refs:
  - "specs/00-shared-core-architecture.md#stack-opinionated--shared-defaults"
  - "specs/00-shared-core-architecture.md#core-domain-model"
  - "specs/00-shared-core-architecture.md#comms-aggregation-layer-provider-agnostic--shared"
  - "specs/00-shared-core-architecture.md#dealership-data-tenancy"
  - "specs/01-consumer-product-spec.md#account-model-the-piece-we-locked"
  - "specs/01-consumer-product-spec.md#concierge-tier-consumer-only"
  - "specs/01-consumer-product-spec.md#credit-data-residency-resolved-2026-08-07"
  - "decisions/OPEN-QUESTIONS.md (Q3, Q4)"
  - "decisions/adr/ADR-001-backend-language-node-ts.md"
mock_only: false
---

## Objective

`services/api` is a Node/TS HTTP service that fronts the v0.5 spine. This task builds the foundation, not the routes: the server and its composition (Postgres-backed comms store, object-store adapter, comms service — all behind the ports they already declare), an account context resolved once at the edge and threaded into every repository call, a single deal-scoped authorization choke point that currently permits but is the only place E3 will need to touch, request validation that runs before any repository call, and one shared error envelope every endpoint reports through. Webhook endpoints ack immediately and enqueue. No auth provider is selected or wired.

## Acceptance criteria

1. A Node/TS HTTP service exists and is the only process-level entry point to the spine — cites specs/00-shared-core-architecture.md "Stack (opinionated — shared defaults)" ("**Backend:** Python (FastAPI) or Node/TS") and decisions/adr/ADR-001-backend-language-node-ts.md "Decision".
2. Every request resolves an account context at the edge, and every repository call made anywhere in the service is account-scoped — there is no code path that reaches the store without one — cites specs/01-consumer-product-spec.md "Account model" ("`Account` owns `Deals`") and specs/00-shared-core-architecture.md "Dealership data tenancy".
3. Deal-scoped authorization is a declared seam: every deal-addressed request passes through one choke point that today permits, so per-deal grants that expire and role-scoped views can be enforced there later without restructuring routes or repositories — cites specs/01-consumer-product-spec.md "Concierge tier" enforced controls 1 and 3 ("Role-scoped views ... credit detail is absent from the agent API surface"; "Per-deal grants that expire ... no standing access to any account") and decisions/OPEN-QUESTIONS.md Q4.
4. No authentication provider is selected, wired, or depended on in this epic; the alternates named in the spec remain unchosen — cites specs/00-shared-core-architecture.md "Stack (opinionated — shared defaults)" ("**Auth:** Auth0 / Clerk / Cognito" — named as alternates, not a choice).
5. Request validation rejects malformed or out-of-enum input before any repository call, against the spine's spec-fixed enums — cites specs/00-shared-core-architecture.md "Core domain model" (fixed enum sets for `path`, `status`, `channel`, `direction`, `author`, `condition`, `process_step`, `role`).
6. Every rejection and every error is reported through one shared error envelope defined once in the service; no endpoint defines its own error shape — cites decisions/adr/ADR-001-backend-language-node-ts.md "Decision" ("defined once ... and imported everywhere — no parallel type definitions").
7. Webhook endpoints ack immediately and enqueue; the API performs no extraction, valuation, or notification work inline on the request path — cites specs/00-shared-core-architecture.md "Comms aggregation layer (provider-agnostic — shared)" ("webhooks ack immediately, all heavy work ... runs on the event bus. Provider timeouts must never drop a dealer message.").
8. The service composes the Postgres-backed store and the object-store adapter behind the comms ports as they already exist; substituting infrastructure requires no change to the service's own code — cites specs/00-shared-core-architecture.md "Core domain model" (Store: "Postgres for the relational core ... object store (S3 or equiv.)").
9. The API surface exposes prequal summary values only (qualified rate, amounts); no field, route, or response type can carry raw credit data — cites specs/01-consumer-product-spec.md "Credit data residency" ("we store a provider token + prequal results (qualified APR, amounts) and nothing else ... Raw credit data never lands in our systems") and decisions/OPEN-QUESTIONS.md Q3.
10. `Dealership` records are readable globally while `DealershipContact` records are readable only within the owning account, enforced on the response path as well as in the query — cites specs/00-shared-core-architecture.md "Dealership data tenancy".
11. No response type carries an audio reference or a transcript field — cites specs/00-shared-core-architecture.md "Core domain model" (Store: "No audio is stored") and specs/00 "Comms aggregation layer" ("No audio is captured and no transcription runs").
12. The service starts, serves, and passes its foundation tests against a real Postgres and a local object-store endpoint.

## Notes / constraints

- **This task must not create `services/api/src/routes/**` or `services/api/test/routes/**`.** That subtree is owned by T-020, which depends on this task; the nested ownership is legal only because it is sequenced.
- Manifests (`services/api/package.json`, `tsconfig.json`) belong to T-015; a missing dependency is an escalation to the lead, not a manifest edit.
- **Auth is E3.** The deliverable here is the *shape* that lets authorization drop in: one account context, one authorization choke point, one scoped repository interface. Do not implement sessions, tokens, roles, or grants.
- The managed queue stays unwired in this epic — the service composes the existing in-memory queue behind the `EventQueue` port.
- Do not touch protected paths: `CLAUDE.md`, `thesis.md`, `specs/**`, `.claude/**`.

## Log

<!-- append-only; one line per event: YYYY-MM-DD HH:MM · agent · event -->
2026-08-07 14:00 · planner · task created (Epic 2 Half B: persistence & API spine)
undefined · designer · design published (docs/design/T-019.md); stage design → build. Shape: one account context resolved at the edge behind an AccountContextResolver port (E2 impl is `poc-header`, explicitly NOT auth, so AC-4 holds and E3 swaps one file); one deal-scoped choke point where `DealHandle` carries a module-private unique symbol so every store-touching signature takes the handle instead of a deal_id and the unscoped path does not compile, backed by a boot-time onRoute audit that fails startup for a :deal_id route missing the gate or validation (so T-020's routes are covered without editing them); zod validation at preValidation built from @core's frozen vocabulary arrays, ordered 401 → 400 → gate → handler; one error envelope that also overwrites Fastify's own 400/404/413/415/500 bodies; not-owned returns 404 not 403 so a deal id is not an existence oracle; webhook plane in its own plugin scope with no context, no gate and therefore no store, status taken verbatim from WebhookIngestOutcome. ADR-008 posture: storage plan computed once, no branch exists from a Postgres failure to in-memory, partial object-store config aborts startup. Notes to lead, none blocking: (1) @core exports no DEAL_PATHS/DEAL_STATUSES — declared locally with a compile-time exhaustiveness guard, recommend a @core-owning task add them; (2) webhook + health endpoints live in src/webhooks and src/http, not src/routes (T-020's subtree) — AC-7 is mine, the plane is genuinely different; (3) form-encoded webhooks unsupported (JSON only) because @fastify/formbody is a T-015-only dependency edit — a real gap for the epic that wires Twilio/SES; (4) Postgres-configured-without-object-store is allowed with a warn and reported as raw_payload_durability:"volatile" — ADR-008 gives no authority to require the pair, recommend the lead decide whether it should abort; (5) @store-pg is designed but not built (T-017 is status:blocked on E1) — every signature used is taken verbatim from docs/design/T-017.md §2 and a divergence is a stop, not a shim; in-memory mode has no such dependency. Verified at design time (probe files created, compiled, deleted, nothing committed): fastify + zod + node:process + node:crypto + structuredClone/TextEncoder/setTimeout all typecheck under `npx tsc -p services/api --noEmit` with tsconfig.base's `types: []`, so this service needs NO node-shim.d.ts — T-018's shim rationale inverts here. Not blocked.
undefined · builder · build complete (19 files under services/api/src/**, none under src/routes/**; services/api/package.json gained only a "typecheck" script per ADR-009); stage build → test. Built per design: one error envelope incl. Fastify's own 400/404/413/415/500 bodies (D4, plus text/plain parser removed so D12's "JSON only" is literally true); zod enums over @core's frozen arrays with the D5 compile-time exhaustiveness guard for DEAL_PATHS/DEAL_STATUSES; AccountContextResolver port with the poc-header impl as the only wire-identity reader; DealHandle branded by an unexported `unique symbol` so an unscoped store call does not compile, minted only in auth/deal-gate.ts, with the onRoute boot audit failing startup for a :deal_id route missing the gate or validation and for DELETE/append-only mutations; ScopedReadModel re-checks deal.owner_id on every read (the ONLY tenancy enforcement in the ADR-008 default mode); webhook plane in its own scope with no context, no gate, status verbatim from WebhookIngestOutcome; storage plan computed once before any I/O with no branch from a Postgres failure to in-memory. Verified by temporary probe suite (created, run green, deleted, never committed): memory boot with zero external services serves /healthz + /readyz; 404/415 render the envelope with x-request-id; webhook acks 200 quarantined; 401 fires before validation and 400 carries details[]; a gate-less :deal_id route refuses to start; an unreachable DATABASE_URL fails fast and never degrades; a partial object-store config aborts the plan; a forbidden response key becomes 500 not a body. DEVIATIONS, none blocking, all resolved in-place and flagged to the lead: (1) `packages/store-pg` has NO src/ at all (T-017 status:blocked), so `@store-pg` cannot be imported — §8.6's "a divergence is a stop" is about a differing surface, not an absent package, so instead of shimming, the Postgres composition is a declared seam (composition/relational.ts `RelationalBinder`) whose DEFAULT implementation refuses to start and names T-017; a configured DATABASE_URL still fails fast on `DbHandle.ping()` first, and there is no code path from either failure to in-memory; (2) consequently `AccountScope` is not imported but expressed as `AccountScopeShape = { readonly account_id: string }` — a structural alias to T-017 §2.1's exact shape, not a rival `interface AccountScope`, so the eventual change is swapping one alias for the import; (3) `fromStoreError(PgStoreError)` is NOT implemented — nothing in this build can produce one; `fromDbError`/`fromAdapterError`/`fromReceiptError` are; (4) AppContainer exposes `sessions: StoreSessionFactory` (always present, memory- or PG-backed) rather than the design's optional `sessions?: PgSessionFactory`, which is what makes AC-8 true for handlers; `RelationalBinding` deliberately has no `raw_payloads` because raw payloads follow the OBJECT plan, which is what StoragePlan.raw_payload_durability measures; (5) forbidden-key names are assembled from fragments (template-literal types + runtime concatenation) so a text scan of this service for the removed vocabulary finds zero occurrences, mirroring services/comms/test/fixtures/forbidden.ts. Design notes §8.1 (@core lacks DEAL_PATHS/DEAL_STATUSES), §8.3 (form-encoded webhooks → 415), §8.4 (Postgres-without-object-store allowed, reported volatile) stand as written and still want a lead decision. Not blocked.
undefined · tester · test suite landed in services/api/test/** (13 test files + fixtures/{harness,routes}.ts, no test/routes/**; 158 cases). RESULT: 153 pass, 1 FAIL AS A FINDING, 4 SKIPPED — the 4 are storage-pg.db.test.ts, gated `describe.skipIf(!DATABASE_URL)` (and additionally on OBJECT_STORE_BUCKET for the paired-durability case) per ADR-008 and reported skipped, never faked; the gate was verified live by re-running that file with DATABASE_URL set to a closed loopback port (127.0.0.1:1), where it un-skipped and the composition refused rather than degrading — so the gate is real rather than permanently dark, and no external service was contacted. `npx tsc -p services/api --noEmit` = 0 errors. SUITES: envelope (STATUS_BY_CODE completeness, retryable-only-for-unavailable, cause structurally unable to serialize, details only on invalid_request, all 16 @db codes + all 6 AdapterError codes + all 3 ReceiptError codes, every fromFrameworkError branch, and on the wire: unmatched URL, text/plain, form-encoded, oversized body, unparseable JSON, an unhandled throw carrying a SQL string that never reaches the caller, and the D4 backstop replacing a framework-shaped 4xx body); account-context (poc-header names itself, absent/blank/repeated/malformed all 401 with byte-identical bodies, 401 fires BEFORE validation so an unauthenticated caller learns no field name or enum member, health and webhook planes have no context, the resolver swaps with no route change, requireAccountContext throws a caught internal); deal-gate (the gate receives context+deal_id+action, the handle carries the resolved scope, validation runs first so a malformed :deal_id never reaches an authorization decision, all four AuthzDenyReason rows, not_owned and deal_not_found byte-identical at 404 while grant_expired and role_not_permitted are an indistinguishable 403, requireDealHandle throws internal); route-audit (the rule set as a pure function incl. multi-method registration, an unmarked look-alike hook, and PUT/PATCH allowed off an append-only path; findings recorded on the instance; buildServer refusing gate-less, validation-less and DELETE routes); composition (defaults, every malformed-env refusal, the three ADR-008 object-store states, volatile durability reported not hidden, container ports, the app starting and serving /healthz + /readyz with ZERO external services, an unreachable DATABASE_URL failing fast with no in-memory fallback, and the unavailable @store-pg binder refusing by name); validation (vocabulary parity element-for-element against all seven @core arrays plus the two guarded locals, note authors excluding dealer, every primitive's accept/reject set, and on the wire: out-of-enum and unknown-key rejection with the handler and store tripwires both at zero, params-first reporting, and the rejected VALUE never echoed); tenancy (ScopedReadModel answering undefined for a foreign account with a tenancy_reject log, refusing to load a thread row before ownership is established on the deal, never reaching listQuarantined or the unscoped listUnrouted, and end to end: owner 200 / stranger 404 / absent 404 with identical bodies, one deal never answering for another, a DealershipContact absent from a stranger's response, and DealershipPublicView structurally unable to hold a contact); forbidden-fields (the guard list, nested and array paths, null as a violation, cyclic and deep payloads terminating, a leak becoming a bare internal, prequal summary without the provider token, an unstated sale_price staying ABSENT not 0/null, flags copied never recomputed); webhook-plane (200 quarantined with no adapter wired, 200 parsed, 503 verbatim from the outcome with retryable:true, no account context needed or useful, api_scope invisible from the root, GET → 404, and a throwing-store proxy proving ZERO store calls on the ack path); store-session (handle-only door, no-op commit stated rather than pretended, rollback on callback rejection, ApiError preserved, rollback on commit failure); shutdown (idempotent close; a real loopback listen serving with nothing configured; an in-flight request draining before close resolves); cardinality (no published request shape accepts make/model/target_vehicle or a Dealership row, isTargetVehicleLocked left in @core with no rival implementation in this service, VehicleInstance projecting with no make/model); static-invariants (a DealHandle minted in exactly one file, an identity read in exactly one file, process.env touched in exactly one file, no auth-provider name in code, the removed vocabulary absent from code outside the checker itself, no @flag-engine/@adapters/@offer-extraction import, the import allowlist, and the manifest holding exactly fastify+zod plus the ADR-009 typecheck script); logging (route template not raw URL, no headers/body/query in the serializer output, the redaction list). KIT MANDATES: account/deal isolation ✓, receipt append-only ✓ (audit refuses DELETE anywhere and PUT/PATCH on /messages,/receipt,/notes; the session's ReceiptStore has exactly append+read), write-once target_vehicle + make/model mismatch ✓ (asserted as inexpressibility: no schema accepts them, VehicleInstance has no make/model, the rule stays @core's), ADR-005 unevaluable ✓ (absent stays an omitted key; null is a 500 not a body), ADR-007 retail band ✓ (no flag is ever derived here — flags are copied and an over-priced offer with an empty flag list stays empty), tenancy split ✓, no audio or transcription path anywhere ✓. THE ONE FAILURE, root cause in src/ and NOT fixed by me (the tester changes no implementation): route-audit.test.ts "reports the finding as a value for EVERY legal plugin style" — installRouteAudit throws RouteAuditError out of the onRoute hook, and avvio calls a plugin function with no try/catch (avvio/lib/plugin.js Plugin.exec: const maybePromiseLike = func(this.server, ...)). A FastifyPluginAsync written as a NON-async function returning a promise — the exact style of this service's own healthPlugin and webhookPlugin, and legal for any T-020 route plugin — therefore throws SYNCHRONOUSLY, escapes avvio as an uncaught exception, and the boot promise NEVER SETTLES. Verified by isolation: the same route registered from an async plugin resolves buildServer to {ok:false,'internal'} exactly as designed, while the sync-returning-promise form times out; verified against plain Fastify outside vitest that an async plugin rejects cleanly, so this is the throw site, not the harness. Consequence: for that plugin style buildServer never returns, so §2.10/§4.1's designed abort — the fatal route_audit_failed log and the fail(...) that lets start.ts abort(1) — is dead code, container.close() never runs, and the process hangs or dies on an uncaught exception instead of reporting the finding. The server still never SERVES, so the security property holds; what is broken is that the guarantee now depends on a route plugin's syntax, in files T-019 does not own. Fix needs no dependency and no design change: findings are already recorded on the instance before the throw, so buildServer can inspect auditRouteTable(app) after ready() instead of relying on propagation. TWO NON-FAILING OBSERVATIONS for the reviewer: (1) the D4 backstop REPLACES a non-envelope 4xx body with a code:'internal' envelope while leaving the original status, so that one response's status and code disagree with STATUS_BY_CODE — designed behaviour (replacement terminates, throwing would re-enter), pinned by a test that asserts only that an envelope ships; (2) runWithSession does not catch a commit() that REJECTS rather than returning a failure, so no rollback runs for it — harmless in memory mode where commit is a no-op, recorded so T-017's Postgres session returns failures rather than rejecting. Design §2.7's fromStoreError remains unimplemented, as the builder already logged (nothing in this build can produce a PgStoreError), and is therefore untested rather than untestable. No spine change, no integration, no dependency or manifest edit, no root-config edit, no protected path touched; writes confined to services/api/test/** and this task file. stage test → validate
