---
id: T-014
title: Comms service v0.5 — notes-first capture, call metadata only, transcription stage removed
stage: test
owner_agent: builder
status: in_progress
depends_on: [T-010, T-012]
file_ownership:
  - "services/comms/**"
spec_refs:
  - "specs/00-shared-core-architecture.md#comms-aggregation-layer-provider-agnostic--shared"
  - "specs/00-shared-core-architecture.md#core-domain-model"
  - "specs/00-shared-core-architecture.md#async-backbone-shared"
  - "specs/00-shared-core-architecture.md#dealership-data-tenancy"
  - "specs/01-consumer-product-spec.md#consent--recording-posture-resolved-2026-08-07"
  - "specs/01-consumer-product-spec.md#web-surface-war-room-first"
  - "decisions/OPEN-QUESTIONS.md (Q12 amended, Q14, Q22)"
  - "decisions/adr/ADR-001-backend-language-node-ts.md"
  - "decisions/adr/ADR-006-current-offer-rollup-policy.md"
mock_only: false
---

## Objective

`services/comms` implements the v0.5 aggregation flow. The transcription stage — its consumers, its events, and the `TranscriptStub` port — is gone. An inbound call logs call metadata onto the thread and notifies the owner; the owner's typed **note** is a first-class `Message` (channel `note`, direction `internal`, author `buyer` or `concierge`) that runs through the same extractor as any SMS or email. Threads carry `dealership_id`, `vehicle_instance`, `working_with`, and `process_step`. The ports (`EventQueue`, `CommsStore`, `RawPayloadStore`, `ConsentHook`, `OutboundPort`) keep their swap-seam shape so Half B can substitute Postgres and S3 implementations without redesign; the in-memory implementations remain the test double.

## Acceptance criteria

1. The transcription stage is removed end to end: no transcription consumers, no `TranscriptStub` port, no transcript-setting store method, no transcription event subscription — cites specs/01-consumer-product-spec.md "Consent & recording posture" ("No ASR/transcription provider is required, so none is approved or wired") and decisions/OPEN-QUESTIONS.md Q14.
2. Inbound call handling follows the v0.5 flow: log call metadata (time, direction, party) on the `DealerThread` → notify owner → owner writes a note → run offer-extraction on the note — cites specs/00-shared-core-architecture.md "Comms aggregation layer (provider-agnostic — shared)" ("**Inbound call:** `provider webhook → Comms service → log call metadata (time, direction, party) on DealerThread → notify owner → owner writes a note → run offer-extraction on the note.`").
3. No audio is captured or stored on any path, and no code path can produce a recording reference — cites specs/00-shared-core-architecture.md "Comms aggregation layer" ("No audio is captured and no transcription runs") and specs/00 "Core domain model" (Store: "No audio is stored").
4. The consent hook no longer gates a transcription decision; whatever per-product seam remains does not exist to authorise recording or transcription — cites specs/01-consumer-product-spec.md "Consent & recording posture" ("Legal exposure from two-party-consent states is **avoided entirely** rather than managed — there is nothing to consent to").
5. Inbound SMS and email keep the flow `webhook → thread onto DealerThread → extract offer` — cites specs/00-shared-core-architecture.md "Comms aggregation layer" ("**Inbound SMS / email:** `webhook → thread onto DealerThread → extract offer.`").
6. A note is accepted as a first-class message (`channel: note`, `direction: internal`) and is run through the same extractor entry point as SMS and email, with the extracted offer attached to the message — cites specs/00-shared-core-architecture.md "Core domain model" (`Message.channel (call | sms | email | note)`, `direction ... internal = the buyer's/operator's own record`) and "Comms aggregation layer" ("any message text — buyer note, SMS, or email — → parsed `Offer` ... attached to the message").
7. Every stored `Message` carries an explicit `author` (`dealer | buyer | concierge`); the service never infers it — cites specs/00-shared-core-architecture.md "Core domain model" (`author ... who produced this text — never inferred`) and decisions/OPEN-QUESTIONS.md Q22.
8. Thread resolution produces/updates a `DealerThread` carrying `dealership_id` (global `Dealership` reference), `vehicle_instance`, `working_with` (account-private `DealershipContact`), and `process_step` — cites specs/00-shared-core-architecture.md "Core domain model" (`DealerThread` block) and decisions/OPEN-QUESTIONS.md Q12 (AMENDED).
9. A `DealershipContact` is never read or written outside the account that owns it, and `Dealership` records are treated as globally shared — cites specs/00-shared-core-architecture.md "Dealership data tenancy".
10. `current_offer` is rolled up by per-field newest-message-wins accumulation, unchanged — cites decisions/adr/ADR-006-current-offer-rollup-policy.md "Decision".
11. Webhooks ack immediately and all remaining heavy work runs on the event bus, so a provider timeout can never drop a dealer message — cites specs/00-shared-core-architecture.md "Comms aggregation layer" ("webhooks ack immediately, all heavy work ... runs on the event bus. Provider timeouts must never drop a dealer message.") and specs/00 "Async backbone (shared)".
12. The ports keep their shape as swap seams to Postgres, the object store, and a managed queue; no port signature encodes an in-memory assumption — cites specs/00-shared-core-architecture.md "Core domain model" (Store: "Postgres for the relational core ... object store (S3 or equiv.)") and specs/00 "Async backbone (shared)".
13. `identity_ref` routing stays provider-agnostic and exact-match; the service does not learn who provisioned an identity — cites specs/00-shared-core-architecture.md "Core domain model" ("`identity_ref` is deliberately provider-agnostic").
14. No spine type is redefined; everything domain-shaped is imported from `packages/core` — cites decisions/adr/ADR-001-backend-language-node-ts.md "Decision".
15. The end-to-end pipeline tests pass with no external infrastructure, using the in-memory implementations.

## Notes / constraints

- Depends on T-012 because the service invokes the extractor; the extractor's note-shaped entry point must land first.
- The managed queue is **not** wired in this epic — the in-memory queue stays. Only the store and raw-payload seams are filled in Half B (T-017, T-018).
- Outbound remains type-only. Origination/relay is a later epic; do not implement sending here.
- Owns `services/comms/**` only. Root workspace config belongs to T-015; the HTTP surface belongs to T-019.
- Do not touch protected paths: `CLAUDE.md`, `thesis.md`, `specs/**`, `.claude/**`.

## Log

<!-- append-only; one line per event: YYYY-MM-DD HH:MM · agent · event -->
2026-08-07 14:00 · planner · task created (Epic 2 Half A: v0.5 domain migration)
undefined · designer · design doc published (docs/design/T-014.md). Measured starting state first: npx tsc -p services/comms --noEmit = 52 errors across src/ and test/, in exactly five families, every one a v0.5 spine change this task absorbs (transcription contracts gone from @core; Message.author now required; Message.transcript gone; DealerThread reshaped to dealership_id/vehicle_instance/working_with/process_step; event payloads renamed dealer_id→dealership_id and provider_message_ref→message_ref; InboundComms.call_ref replaced by call_meta). Nothing in that list is a BEHAVIOR defect — ack-then-queue, the belt-and-braces idempotency ledger, keyed writes, exact-match identity routing and the ADR-006 rollup are correct as built and are preserved verbatim; this is a re-basing, not a rewrite, and intake.ts is explicitly NOT TOUCHED so the one rule specs/00 states outright cannot drift. DESIGN: (1) TranscriptStub, both transcription consumers, CommsStore.setTranscript, CommsServiceDeps.transcribe and the two subscriptions are DELETED not stubbed (D1) — @core has no transcription event type, so publishing one is now a compile error, and a dormant port is an unapproved-integration socket (no ASR provider is approved, Q14). Five consumers become three. (2) ConsentHook SURVIVES with its name and single method but returns void (D2): AC-4 forbids it gating a transcription decision and { transcribe: boolean } is literally a transcription authorisation, so the decision goes and the per-product seam stays; it is invoked AFTER the call message is durably appended and cannot suppress anything (D3), and a throwing hook yields bounded retries → DLQ with the call still threaded exactly once. (3) Inbound call = log call metadata → notify owner, and nothing else: Message{channel:'call', direction:'in', author:'dealer', call_meta, NO body}, then alert.dispatch.requested.v1 kind 'call_logged' (the @core kind that exists for exactly this prompt), and NO extraction request because there is no text. call_meta is copied verbatim when the adapter supplied one; when absent it is derived from received_at plus from.phone/email — both provider-supplied facts already in the envelope, not inferences — and NEVER a fabricated duration_seconds (D12). (4) The note is a new first-class service surface, NoteIntake.submitNote, not an InboundComms: @core's InboundChannel = Exclude<MessageChannel,'note'> makes a provider-sourced note unrepresentable, and SpineEventType is frozen, so the note's heavy work rides the existing offer.extraction.requested.v1 whose message_ref the spine already documents as "caller-generated ref for a note" (D4) — no event type invented, no core edit. Notes append SYNCHRONOUSLY then enqueue extraction (D5): ack-then-queue exists so a provider timeout cannot lose a message we have not stored, and a note has no provider and no retrying counterparty, so returning "saved" before it is durable is the failure mode here; the heavy half still runs on the bus (AC-11). NoteSubmitOutcome encodes the HTTP status 1:1 like WebhookIngestOutcome, including a distinct recorded_extraction_deferred/503 for "note IS stored, only the enqueue failed" — the caller retries the same client_note_ref, the keyed append no-ops, only the publish is re-attempted. (5) author is never inferred (AC-7): provider-inbound is the constant 'dealer' read off the TRANSPORT not off content (D6 — including the bare call record, where there is no text for the honesty risk to attach to; deliberately different from T-012 D7, where a RECEIPT call_meta entry's author answers "who logged this account-side", and neither answer may be copied onto the other), while a note's author is caller-supplied, required, typed Exclude<MessageAuthor,'dealer'>, and direction is not accepted from the caller at all — the service fixes 'internal', so a dealer-authored or non-internal note is unrepresentable rather than merely rejected. (6) Thread resolution is TWO steps (D9): account-private contact → dealership_id via a per-deal index (bindThreadContact/resolveDealershipByContact both take deal_id FIRST, so an unscoped cross-account contact lookup is inexpressible — AC-9), then dealership_id → thread row created on first contact at process_step 'information_gather'. The service NEVER mints a Dealership: that table is globally shared, and minting dealership:+1555… from an unrecognised number would inject account-guessed rows into the shared namespace the tenancy split exists to protect. Consequence and the one behavior change from T-009: a known deal + unknown dealership contact lands in the unrouted holding area with a new reason 'no_thread_match' carrying deal_id (D10) — held whole, operator-visible, replayable, never dropped and never guessed. No port method can create or edit a Dealership, and none can mutate process_step, working_with, vehicle_instance or target_vehicle: a message arriving cannot advance a negotiation step. (7) StoredMessage.provider_message_ref → message_ref with source:string replaced by a discriminated origin: {kind:'provider';source} | {kind:'in_app'} (D8), so a fabricated adapter id on an in-app note is unrepresentable (T-012 D9's rule applied here), and in-app refs are namespaced note:<client_note_ref> so a client-chosen ref can never collide with a provider id in the per-deal index and silently swallow one of the two as a "duplicate". (8) ADR-006 rollup untouched apart from that rename; flags stays [] because this service never evaluates a flag. Per-operation error/retry/idempotency/logging tables for intake, all three consumers and submitNote, incl. the never-logged list (bodies, note text, subjects, phone numbers, aliases, raw payloads AND call_meta.party — the one PII-shaped field a v0.5 call record legitimately stores) which T-019 inherits verbatim; 16 invariant touchpoints incl. the quarantine-stash-is-not-an-audio-back-door row a reviewer will ask about (the only reader is operator replay, and replay re-enters through parseInboundWebhook whose output type has no field an audio reference could land in; there is no HTTP client in the service); 14-item test plan, all in-memory, no DATABASE_URL, no network; AC 1-15 trace. FOUR RESOLVED DEVIATIONS, none blocking: D2 keeping ConsentHook with a void return (AC-4 permits deletion, the task objective lists it among the ports that keep their shape; reversal is one interface + one const + one dep field + one call site), D6 author:'dealer' on a call record (reversal: one constant), D10 unrouted-instead-of-auto-thread (forced by v0.5 tenancy; nothing is lost), and D13 a note-length bound (injectable, default 20 000 chars) with provider-inbound text explicitly NEVER capped or truncated — T-012 §4.1 assigned size-bounding to this boundary and forbade the extractor from truncating, and the asymmetry is asserted as a test because a truncated dealer message is silently altered evidence. Receipt wiring is deliberately NOT built (D14): the ACs do not ask for it and it would need a services/comms/package.json dependency edit that ADR-009 confines to T-015; T-012 §4.6 already fixes the rules the eventual writer inherits. No spine change, no integration, no dependency or package.json edit, no root-config edit, no protected path touched; writes confined to docs/design/T-014.md and this task file. stage design → build
undefined · builder · built EXACTLY per docs/design/T-014.md; src is green (npx tsc --noEmit over services/comms/src = 0 errors, down from the design's measured 52 across src+test). D1 executed as DELETION not stubbing: the TranscriptStub port, both speech-stage consumers (transcription-stub-worker.ts, transcription-apply.ts), CommsStore.setTranscript, CommsServiceDeps.transcribe and the two subscriptions are gone; five consumers became three; a repo-wide grep for transcript|recording|asr|call_ref under services/comms/src returns ZERO hits including comments (the design's standing gate). ConsentHook kept its name and single method and now returns void (D2/AC-4) and is invoked AFTER the durable append (D3), wrapped so a throwing hook yields retry -> bounded -> DLQ with the call still threaded exactly once. Inbound call = Message{channel:'call',direction:'in',author:'dealer',call_meta,NO body} -> alert kind 'call_logged' -> hook, and NO extraction request (no text); call_meta verbatim when the adapter supplied one, else {started_at: received_at, party: from.phone ?? from.email} and NEVER a duration_seconds (D12/ADR-005). notes.ts is the new first-class surface (D4): NoteSubmission fixes direction:'internal' service-side and requires author typed Exclude<MessageAuthor,'dealer'> (D7/AC-7), appends synchronously THEN enqueues offer.extraction.requested.v1 with channel 'note' (D5), and NoteSubmitOutcome encodes the HTTP status 1:1 incl. recorded_extraction_deferred/503. Note and sms/email converge on one event type, one consumer, one extractOffer call, one fill-once attach, one ADR-006 rollup - no note-specific extraction branch (AC-6). Store re-based on Q12 AMENDED: threads carry dealership_id/vehicle_instance/working_with/process_step, created once at 'information_gather' and never advanced by a message arriving; contact routing is deal-scoped (bindThreadContact/resolveDealershipByContact take deal_id FIRST, so a cross-account lookup is inexpressible - AC-9); no port method can create or edit a Dealership, so a known deal + unknown sender lands in the unrouted area under the new reason 'no_thread_match' carrying deal_id (D10). StoredMessage.provider_message_ref -> message_ref with a discriminated origin {kind:'provider';source}|{kind:'in_app'} (D8); in-app refs namespaced note:<client_note_ref>. intake.ts LOGIC IS BYTE-FOR-BYTE UNCHANGED (ack-then-queue, the single deliberate 503, the deterministic quarantine key); the only edit was refreshing its stale spec quote, which still named a transcription stage the current specs/00 line does not. Rollup untouched apart from the field rename (ADR-006). Verified with a THROWAWAY scratch harness (run, then deleted - no test file was added to the repo; the tester owns test/): inbound call logs metadata with exactly one call_logged alert, no extraction and no current_offer; the full v0.5 loop call -> note -> extraction -> rollup lands on the same thread; note idempotency (same client_note_ref twice = one row, disposition 'duplicate'); induced publish failure yields recorded_extraction_deferred with the note READABLE and a retry completing with no second row; all four rejection/404 paths write nothing; unknown identity -> no_identity_match and known deal + unknown sender -> no_thread_match with no new thread; consent hook fires for calls only. THREE RESOLVED DEVIATIONS, none blocking, all logged in the report: (1) services/comms/test/** was left UNTOUCHED and is still red (25 errors, all in exactly the harness families docs/design/T-014.md §5 lists as the tester's re-basing work: TranscriptStub import, Message.author, Message.transcript, VehicleTarget.year, dealer_id -> dealership_id, transcribe dep) - the builder does not write tests, so `npx tsc -p services/comms --noEmit` stays red until the tester lands §5; src alone is green. (2) The comment-only refresh of intake.ts, whose header quoted a superseded specs/00 line and would otherwise fail the design's own zero-hit grep gate; no code line changed. (3) Seeded thread messages get origin read off the message's own channel (note -> in_app, else provider with an empty source) rather than a fabricated adapter id - the design does not specify the seeding path and both alternatives assert something untrue. No spine change, no integration, no dependency or package.json edit, no root-config edit, no protected path touched; writes confined to services/comms/src/** and this task file. stage build -> test
