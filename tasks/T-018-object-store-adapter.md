---
id: T-018
title: S3-compatible object store adapter — attachments, documents, dossiers (no audio)
stage: validate
owner_agent: builder
status: in_progress
depends_on: [T-012, T-015]
file_ownership:
  - "packages/object-store/src/**"
  - "packages/object-store/test/**"
  - "packages/object-store/package.json"   # scripts field ONLY, per ADR-009
spec_refs:
  - "specs/00-shared-core-architecture.md#core-domain-model"
  - "specs/00-shared-core-architecture.md#integrations--anti-corruption--adapter-layer-shared"
  - "specs/00-shared-core-architecture.md#receipt-layer-trust-engine"
  - "specs/01-consumer-product-spec.md#account-model-the-piece-we-locked"
  - "specs/01-consumer-product-spec.md#consent--recording-posture-resolved-2026-08-07"
  - "decisions/adr/ADR-001-backend-language-node-ts.md"
mock_only: false
---

## Objective

`packages/object-store` provides one internal interface for durable object storage over an S3-compatible backend, holding exactly what the spec assigns to the object store: email attachments, uploaded documents, and generated dossiers. Keys are account-scoped, no read path crosses an account, and **no code path can store audio**. Callers see the internal interface only — the SDK's shape never reaches them — so a different S3-compatible provider is a wiring change.

## Acceptance criteria

1. A single internal object-store interface is exported and no caller ever sees the provider SDK's request/response shape — cites specs/00-shared-core-architecture.md "Integrations — anti-corruption / adapter layer (shared)" ("Every external feed sits behind one internal interface. Core services never see a provider's shape.").
2. The adapter stores and retrieves email attachments, uploaded documents, and generated dossiers — cites specs/00-shared-core-architecture.md "Core domain model" (Store: "object store (S3 or equiv.) for email attachments, uploaded documents, and generated dossiers").
3. There is no audio path: no method, content type allowlist entry, or key convention permits storing a recording, and this is covered by test — cites specs/00-shared-core-architecture.md "Core domain model" (Store: "No audio is stored") and specs/01-consumer-product-spec.md "Consent & recording posture" ("no audio is ever captured or stored").
4. Object keys are account-scoped and every read is scoped to the requesting account, so one account's artifacts are not addressable from another — cites specs/01-consumer-product-spec.md "Account model" ("`Account` owns `Deals`") and specs/00-shared-core-architecture.md "Dealership data tenancy" (the tenancy principle: private data never exposed to another account).
5. A stored object is addressed by a stable reference that Postgres rows can carry, so the relational core holds references and the object store holds bytes — cites specs/00-shared-core-architecture.md "Core domain model" (Store split between Postgres and the object store).
6. A generated deal dossier can be persisted and retrieved as a durable, exportable artifact; rendering it remains the receipt package's concern — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" ("Generates a shareable **deal dossier** (PDF + web link)" and "exportable").
7. Stored dossier and document artifacts are never mutated in place — a revision is a new object, so the receipt trail's append-only property is not undermined by the storage layer — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" ("append-only").
8. The adapter defines no spine type of its own; anything domain-shaped is imported from `packages/core` — cites decisions/adr/ADR-001-backend-language-node-ts.md "Decision".

## Notes / constraints

- Tests run against a locally provisioned S3-compatible endpoint or an in-process fake. **No cloud credentials belong in the repo** — credential absence is the structural enforcement the constitution relies on.
- Depends on T-012 because the dossier structure it persists is defined by the realigned receipt package.
- Manifests (`packages/object-store/package.json`, `tsconfig.json`) belong to T-015; a missing dependency is an escalation to the lead.
- No CDN, signing service, or image-processing service is introduced — those are unlisted integrations and would require escalation.
- Do not touch protected paths: `CLAUDE.md`, `thesis.md`, `specs/**`, `.claude/**`.

## Log

<!-- append-only; one line per event: YYYY-MM-DD HH:MM · agent · event -->
2026-08-07 14:00 · planner · task created (Epic 2 Half B: persistence & API spine)
2026-08-07 · designer · design complete → docs/design/T-018.md; stage design → build. Notes to lead: D5 (RawPayloadStore is sync, S3 is not — resolved as write-behind within ownership; recommend a follow-up task make the T-014 port async) and D6 (@object-store → @comms import edge, authorized by ADR-009 §1, amends T-015 §2.2). Not blocked.
2026-08-07 · builder · build complete → packages/object-store/src/{contract,keys,content-policy,backend,memory-backend,s3-backend,artifact-store,config,raw-payloads,index}.ts + node-shim.d.ts; scripts.typecheck added to the package manifest (ADR-009 §2, dependencies untouched); `npx tsc -p packages/object-store --noEmit` green; no tests written (tester stage owns test/**). Stage build → test. One deviation from design §7, resolved within ownership and reported to the lead: a `src/node-shim.d.ts` WAS required — T-015 §5.7's premise that @types/node reaches the program transitively is false here (verified: `node:crypto` is unresolved even with @aws-sdk/client-s3 imported), and the duplicate-identifier collision it guarded against cannot occur because an ambient .d.ts is included by a tsconfig glob, never by an import, so no program sees two shims. Design notes D5 (write-behind window) and D6 (@object-store → @comms edge) stand as written. Not blocked.
undefined · tester · test suite landed in packages/object-store/test/** (11 files + helpers.ts + a test-scope node-shim.d.ts; 359 cases). RESULT: 348 pass, 3 FAIL AS FINDINGS (2 root causes), 8 SKIPPED — the 8 are s3-live.test.ts, gated `describe.skipIf(!OBJECT_STORE_BUCKET)` per ADR-008 and reported skipped, never faked; the gate was verified LIVE by running that file with OBJECT_STORE_BUCKET/REGION set and OBJECT_STORE_ENDPOINT pointed at a closed loopback port (127.0.0.1:1) — the suite un-skipped and the endpoint-dependent cases returned provider_unavailable, so the gate is real rather than permanently dark, and no external host was contacted and no credential exists in this repo. `npx tsc -p packages/object-store --noEmit` = 0 errors. T-018 has no DB-dependent test, so the DATABASE_URL rule has no subject here. SUITES: keys (content addressing incl. published sha-256 vectors, the ^[A-Za-z0-9_-]{1,64}$ path-traversal guard on both ids, parse rejection of every malformed ref, isRefOwnedBy incl. the acme/acmex prefix-neighbour case, refPrefix leaking 12 chars not a capability, ops/raw-payload/ unparseable as an artifact ref); content-policy (per-kind literal allowlists, normalization on the allow AND deny paths, the §4.3 step ORDER pinned by which message comes back, the size cap at and one byte over, the RIFF trap in both directions, metadata sanitizer incl. CRLF header-splitting and the undefined-not-empty-string rule); no-audio (never skipped: the 3-literal kind union, 12 declared audio/video types × all 3 kinds, all 11 magic deny signatures each mislabeled as application/pdf AND as text/plain, plus a coverage gate that fails if the deny list grows without a fixture, and surface scans proving no export/method name is audio-shaped); artifact-store (put/get/head/list happy paths, content-type normalization, absent optionals OMITTED not undefined, buffer copy in both directions, HEAD-first no-rewrite proven by counting backend puts, revision-is-a-new-object, every §4.1/§4.4 provider row incl. hash-vs-key integrity failure, all four metadata cross-checks, missing content type, and log events carrying no bytes/no full ref/no filename); tenancy (cross-account get/head → not_found with the SAME code+retryable as a genuine miss, decided locally with ZERO backend calls, warn-level object_store.tenancy.reject logged, list confined to acct/{account}/, acme vs acmex, deal-scoped list, identical bytes in two deals = two objects, ops/ key space unreachable from an account-scoped read); append-only (absence asserted as hard as behaviour: no delete/copy/move/putAtKey/presign on the store, the backend seam, or the barrel; no DeleteObjectCommand/CopyObjectCommand/s3-request-presigner in comment-stripped src; backend write is a no-op for an existing key; dossier persisted + traced to its receipt_bundle_id while rendering stays in @receipt); config (the THREE ADR-008 states kept distinct — undefined ⇒ default in-memory, {ok:false,'auth'} ⇒ fail fast never degrade, ok ⇒ S3; no fourth env name; no credential-shaped field; no process.env read inside the package); backend-errors (all 20 name rows, 4 syscall codes, 6 HTTP statuses, statusCode fallback, name-beats-status, 8 unrecognized shapes → retryable provider_unavailable, and toAdapterError proven never to copy a provider message body — an error whose text carried AKIA…, a foreign acct/ key and a walk-away number came back as `get failed: AccessDenied`); raw-payloads (ref bit-identical to @comms InMemoryRawPayloadStore over 8 payloads incl. key-order permutation, stash non-blocking against a gated backend, one write per redelivery, write-behind retry/terminal-failure/failedRefs/backlog-alarm-once, cache eviction with fetch still durable, all four fetch failure modes, and the payload proven absent from every log line); surface (@aws-sdk imported by exactly one file — s3-backend.ts — with the barrel exporting no client/command/stream, the import allowlist, the manifest holding exactly @aws-sdk/client-s3 + the ADR-009 typecheck script, no spine type redefined, and §5.11 absence of above_market/target_vehicle/current_offer/sale_price/walk_away/process_step so ADR-006/ADR-007 cannot be contradicted by the storage layer). KIT MANDATES: account/deal isolation ✓ (tenancy suite), receipt append-only ✓ (append-only suite; no update or delete path exists), write-once target_vehicle + make/model mismatch ✓ (not applicable here and NOT faked — asserted as absence: no make/model/VIN/price/band token appears anywhere in this package), ADR-005 unevaluable ✓ (a get miss is not_found never empty bytes; ObjectSummary omits content_type/filename rather than defaulting them; a truncated list says so; absent filename/origin_ref omitted not ''), ADR-007 retail band ✓ (no valuation logic here — asserted absent, with the reason stated), tenancy split ✓, no audio/transcription path anywhere ✓. THE THREE FAILURES, both root causes in src/ and NEITHER fixed by me (the tester changes no implementation): (1) no-audio.test.ts ×2 — `put` with a kind outside ArtifactKind (e.g. 'recording' from the untyped T-019 HTTP edge, or '') THROWS `TypeError: Cannot read properties of undefined (reading 'includes')` at content-policy.ts:151, because `isAllowedForKind` indexes ALLOWED_CONTENT_TYPES[kind] before anything validates the kind. §4.3 orders the kind check (buildArtifactRef, which already returns 'kind is not an artifact kind') AFTER checkContentPolicy, so that refusal is unreachable and the adapter breaks the never-throw-across-the-boundary contract (@core AdapterResult). Nothing is stored, so it is a crash not a breach; the no-audio guarantee itself holds (a 'recording' kind carrying audio/mpeg is still refused as a value by the step-2 media deny). Fix is one guard: reject a non-ArtifactKind kind in step 1 alongside the ids, or make isAllowedForKind total. (2) artifact-store.test.ts ×1 — on the `already_present` branch, put() builds its PutResult metadata with `metadataFromHead(ref, location, existing.value, content_type)` where content_type is the REQUEST's normalized type rather than `existing.value.content_type`; every other field on that branch comes from the HEAD. Two allowlisted types share a magic requirement (text/plain and text/csv both only forbid a NUL byte), so the same bytes re-put under the second label return metadata claiming text/plain while head()/get() report the stored text/csv — a value invented rather than retrieved, against design §5.6, and a caller persisting PutResult.metadata into a Postgres row would serve the wrong Content-Type forever. Fix is one argument. ONE NON-FAILING OBSERVATION for the reviewer, deliberately not made red because the design states only the CODE: a cross-account read returns message 'get failed: no such object' while a genuine miss returns the backend's 'get failed: no such key', so the caller-visible messages differ even though both are not_found — §4.4's stated purpose ("a cross-account probe cannot confirm that an object exists") is defeated if T-020 surfaces AdapterError.message. The test pins code+retryable equality; aligning the two messages would close it. No spine change, no integration, no dependency or manifest edit (the ADR-009 typecheck script was already added by the builder and is only asserted, not touched), no root-config edit, no protected path touched; writes confined to packages/object-store/test/** and this task file. stage test → validate
undefined · fixer · all 3 failing tests + all 4 verifier findings FIXED in packages/object-store/{src,test}; `npx vitest run packages/object-store` = 367 pass, 0 fail, 8 skipped (s3-live only, ADR-008 env gate, re-verified live: with OBJECT_STORE_BUCKET/REGION set and the endpoint on a closed loopback port the suite un-skips and every endpoint-dependent case returns provider_unavailable as a VALUE — no throw, no external host, no credential); `npx tsc -p packages/object-store --noEmit` = 0 errors. (1) HIGH, never-throw-across-the-boundary: `isAllowedForKind` is now TOTAL (param typed `string`, unknown kind ⇒ false instead of indexing `undefined`), and `put` validates the kind in §4.3 step 1 alongside the ids, returning the refusal `buildArtifactRef` already knew ('kind is not an artifact kind') so it is reachable rather than pre-empted. The §4.3 message ORDER for VALID kinds is untouched, so the step-2 audio deny still wins for a real kind carrying audio/* (content-policy.test.ts order suite green). (2) MEDIUM, §5.6/AC-5: the already_present branch of `put` now reports the STORED content type (and stored_at) rather than the request's — `PutResult.metadata` on that branch is now field-for-field equal to what `head()` returns, pinned by test. (3) LOW, §5.6/ADR-005: `BackendHead.byte_length`/`stored_at` and `BackendListItem.byte_length`/`stored_at` are now OPTIONAL, s3-backend OMITS what the provider omitted instead of fabricating `0`/`clock()` (the `clock` it used for that is gone from that file), and one `resolveHead` helper turns an absent field into `malformed_response` on put/get/head/list — consistent with how the missing-content_type case was already handled. (4) LOW, §4.4: a genuine miss and a cross-account refusal now return the IDENTICAL AdapterError including `message` ('<op> failed: no such object'), normalized in the store, so no probe learns anything from wording and no provider text ('NoSuchKey in bucket …') reaches a caller through T-020. TESTS ADDED (16 cases, my ownership): untyped-kind × content-type matrix incl. '' / '../dossier' / 'DOSSIER' with a no-backend-call assertion; text/plain↔text/csv relabel round trip proving PutResult.metadata === head(); retrieved-not-restamped stored_at; omitted byte_length/stored_at ⇒ malformed_response on get/head/put/list; miss-vs-foreign error equality plus a provider-wording leak test. NO spine change, NO new dependency, NO manifest/root-config/protected-path edit; writes confined to packages/object-store/src/{artifact-store,content-policy,backend,s3-backend}.ts, packages/object-store/test/{artifact-store,tenancy}.test.ts and this task file. Not blocked. Ready for re-verification.
