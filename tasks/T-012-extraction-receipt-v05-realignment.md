---
id: T-012
title: Offer-extraction + receipt layer realigned to v0.5 (notes, author, no transcripts)
stage: validate
owner_agent: tester
status: in_progress
depends_on: [T-010]
file_ownership:
  - "packages/offer-extraction/**"
  - "packages/receipt/**"
spec_refs:
  - "specs/00-shared-core-architecture.md#comms-aggregation-layer-provider-agnostic--shared"
  - "specs/00-shared-core-architecture.md#receipt-layer-trust-engine"
  - "specs/00-shared-core-architecture.md#core-domain-model"
  - "specs/01-consumer-product-spec.md#consent--recording-posture-resolved-2026-08-07"
  - "specs/01-consumer-product-spec.md#concierge-tier-consumer-only"
  - "decisions/OPEN-QUESTIONS.md (Q14, Q22)"
  - "decisions/adr/ADR-001-backend-language-node-ts.md"
  - "decisions/adr/ADR-005-offer-sale-price-optional.md"
mock_only: false
---

## Objective

`packages/offer-extraction` and `packages/receipt` compile and pass against the v0.5 spine. The extractor's input vocabulary is buyer notes, SMS, and email — the transcript-shaped entry point and its fixture framing are gone, and a typed note is processed exactly like any other message. The receipt layer's entry kinds become buyer note / SMS / email / call-metadata record, the `recording_ref` and `transcript` kinds are removed, and **every entry carries its author** so operator- or buyer-written text is never presented as the dealer's. The store contract stays structurally append-only.

## Acceptance criteria

1. The extractor is channel-agnostic over the v0.5 channel set (`call | sms | email | note`) through a single entry point, and never depends on how the text was produced — cites specs/00-shared-core-architecture.md "Comms aggregation layer (provider-agnostic — shared)" ("any message text — buyer note, SMS, or email — → parsed `Offer` ... The extractor is channel-agnostic and never depends on how the text was produced") and specs/00 "Core domain model" (`Message.channel`).
2. No transcript-specific input path, parameter, or type remains in the extractor — cites specs/01-consumer-product-spec.md "Consent & recording posture" ("No ASR/transcription provider is required, so none is approved or wired") and decisions/OPEN-QUESTIONS.md Q14.
3. The transcript fixture corpus is re-based as a **buyer-note** corpus (the buyer typing what the dealer said), preserving the parsing coverage it provided — cites specs/01-consumer-product-spec.md "Consent & recording posture" ("The buyer types what the dealer said, in their own words, and the offer extractor parses the terms out of that text").
4. The extractor's output remains the core `Offer` (price, fees, APR, term, monthly), with `sale_price` optional and partial offers first-class — cites specs/00-shared-core-architecture.md "Comms aggregation layer" ("parsed `Offer` (price, fees, APR, term, monthly)") and decisions/adr/ADR-005-offer-sale-price-optional.md "Decision".
5. Receipt entry kinds cover buyer note, SMS, email, and call-metadata record; the `recording_ref` and `transcript` kinds are removed from the type surface — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" ("Every **buyer note, SMS, email, and call-metadata record** is **append-only, timestamped, exportable**. (No recordings or transcripts exist ...)").
6. Every receipt entry carries an `author` drawn from the ratified `Message.author` set (`dealer | buyer | concierge`) and no entry can be stored without one — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" ("Each entry carries its **author** — buyer, concierge operator, or dealer — so self-authored evidence is never presented as if it came from the dealer") and decisions/OPEN-QUESTIONS.md Q22.
7. A call-metadata receipt entry records time, direction, and party and contains no audio pointer and no transcript text — cites specs/00-shared-core-architecture.md "Comms aggregation layer" ("log call metadata (time, direction, party) on DealerThread") and specs/00 "Core domain model" (Store: "No audio is stored").
8. The store contract remains structurally append-only: append and read only, with no update, delete, overwrite, or truncate operation on any exported type — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" ("append-only, timestamped, exportable").
9. The dossier export structure carries the author of every entry through to the exported artifact, so a reader can distinguish dealer-authored from concierge- or buyer-authored evidence — cites specs/00-shared-core-architecture.md "Receipt layer (trust engine)" and specs/01-consumer-product-spec.md "Concierge tier" ("Operator-authored notes carry the `concierge` author label ... so nothing self-authored is ever presented as if the dealer said it").
10. Bundles remain addressed by `Deal.receipt_bundle_id` — cites specs/00-shared-core-architecture.md "Core domain model" (`Deal ... receipt_bundle_id`).
11. Neither package redefines any spine type; all shared vocabulary is imported from `packages/core` — cites decisions/adr/ADR-001-backend-language-node-ts.md "Decision".
12. Both packages typecheck and their tests pass against the v0.5 spine.

## Notes / constraints

- Owns `packages/offer-extraction/**` and `packages/receipt/**`. These two are grouped because both are pure, both are pure-consumer-of-core, and grouping keeps the migration front sequenced without an artificial dependency edge between them.
- `packages/receipt`'s dossier PDF/web-link rendering stays a stub in this epic; durable storage of a rendered dossier is T-018's object store. Do not implement rendering here.
- The extraction corpus rebase is a **renaming and reframing** exercise — the parsing behaviour that corpus proved must not regress. Keep the assertions, change the provenance.
- Do not touch protected paths: `CLAUDE.md`, `thesis.md`, `specs/**`, `.claude/**`.

## Log

<!-- append-only; one line per event: YYYY-MM-DD HH:MM · agent · event -->
2026-08-07 14:00 · planner · task created (Epic 2 Half A: v0.5 domain migration)
undefined · designer · design doc published (docs/design/T-012.md). Measured starting state first: BOTH packages already typecheck green, so they are semantically stale rather than compile-broken — extractOffer's hand-written `call|email|sms` literal chain silently demotes the v0.5 `note` channel to the `sms` fallback (notes, the primary v0.5 extraction input, are processed as mislabelled SMS with nothing failing), and packages/receipt still exports recording_ref/transcript kinds with no author field anywhere. Design: (1) extraction re-keys per-channel cleanup from MessageChannel to an internal NormalizationProfile (plain|email|spoken) behind one exhaustive Record<MessageChannel,NormalizationProfile>, so channel-agnosticism is structural (no channel value exists below normalization) and a future spine channel is a compile error, not a silent fallback; note→spoken, email→email, sms→plain, call→plain (a v0.5 call carries call_meta, not text — defining a call-text profile would re-create the transcript path in spirit); unknown channel falls back to the plain PROFILE, never to a fabricated 'sms' channel; normalizeTranscript→normalizeSpokenForm and the `call → Message.transcript` doc line deleted. (2) transcripts/ fixtures re-based file-by-file as notes/ with expected blocks byte-identical and the spelled-out number forms (twenty nine grand / four fifty a month / six point nine percent) required to survive the reframing — those forms ARE the coverage; corpus loader validates channel against @core MESSAGE_CHANNELS instead of a local Set (which is why a note fixture would have been rejected by the harness rather than caught). (3) receipt kinds become note|sms|email|call_meta with recording_ref/transcript DELETED not deprecated; kind→channel is now a bijection; author: MessageAuthor is required on the input base with no default and no derivation, and coherence is enforced at the TYPE level first (NoteEntryInput fixes direction:'internal' + author:Exclude<MessageAuthor,'dealer'>; sms/email/call_meta fix direction:Exclude<...,'internal'>; call_meta author excludes dealer and means who logged the call account-side per specs/01 concierge control 4), runtime second for JS/JSON boundaries; CallMetaEntryInput carries the @core CallMeta verbatim so AC-7 ("no audio pointer, no transcript text") is a property of the type rather than a rule; provider_message_ref moves off the base onto provider-originated variants only (a note has no provider id, T-010 D10). (4) ReceiptStore stays exactly append+read, dedupe stays first-write-wins with no conflict error (comparing payloads would put bodies in error messages and would invite an overwrite the interface does not offer); dossier gains NO new field — author reaches the export by riding on ReceiptEntry, and the still-stubbed renderer inherits a binding forward constraint that dropping the author label voids the mitigation specs/01's accepted concierge risk is conditioned on. Per-operation error/retry/idempotency/logging tables for extractOffer (total, no error channel, determinism IS the idempotency posture, no input-length cap because truncation is a false negative where the product's promise lives), append (incl. missing-author and coherence-violation rows, both invalid_input with nothing written), read, and the three dossier operations; 13 invariant touchpoints; 17-test plan incl. the test that would have caught today's note bug; AC 1-12 trace. Interpretive calls D1-D13 logged. Five resolved deviations noted, none blocking: D6/D7 type-level authorship narrowing is stricter than the ACs literally require (one-line reversal preserved), D2 call→plain, D9 no provider ref on notes, D11 no new dossier field, plus a forward note that T-016/T-017 must ship author NOT NULL + CHECK and no recording_ref/transcript columns (no migration written here). No spine change, no integration, no dependency edit (ADR-009), no protected path touched. stage design → build
undefined · builder · implemented docs/design/T-012.md exactly, both packages green. EXTRACTION: normalize.ts now owns NormalizationProfile ('plain'|'email'|'spoken') plus an exhaustive Readonly<Record<MessageChannel,NormalizationProfile>> PROFILE_BY_CHANNEL (note→spoken, email→email, sms→plain, call→plain per D2); normalizeText takes a PROFILE, so no channel value exists below normalization and no rule can branch on one (D1, AC-1). extract.ts's `call|email|sms` literal chain and its `: 'sms'` fallback are gone — an unrecognized runtime channel resolves through Object.hasOwn to the plain profile and NEVER to a fabricated channel (D3), which is the silent bug the design measured (note was being processed as mislabelled SMS). normalizeTranscript→normalizeSpokenForm, the section banner reframed to spoken-form typed notes, the `call → Message.transcript` doc line deleted. test/fixtures/transcripts/ re-based file-by-file as notes/ (note-full-offer, note-grand-otd, note-spoken-monthly, note-vague-no-numbers, note-scheduling-no-offer) with every `expected` block byte-identical and every spelled-out/grand/point/percent number form preserved — the coverage survived the reframing unchanged (D12, AC-3). Corpus loader group list is ['notes','sms','email','hostile'] and its channel check now validates against @core MESSAGE_CHANNELS instead of a local Set (D13). RECEIPT: kinds are note|sms|email|call_meta; the audio-pointer and verbatim-call-text kinds and their input types are DELETED from contract.ts and from the index.ts export list (D4, AC-5), CHANNEL_BY_KIND is now a bijection (note→note, sms→sms, email→email, call_meta→call). `author: MessageAuthor` is required on the input base with no default and no derivation (D5, AC-6); coherence is type-level first — NoteEntryInput fixes direction:'internal' + author:Exclude<MessageAuthor,'dealer'>, the three provider-originated variants fix direction:Exclude<MessageDirection,'internal'>, CallMetaEntryInput narrows author the same way (D6/D7) — and runtime second for JS/JSON boundaries (missing/unknown author, internal+dealer, note with non-internal direction, non-note with internal direction, call_meta+dealer → invalid_input, nothing written). CallMetaEntryInput carries the @core CallMeta verbatim and has no body, so AC-7 is a property of the type (D8). provider_message_ref lives only on provider-originated variants; a note has none (D9). ReceiptStore stays exactly append+read, entries defensively copied (nested call_meta included) and deep-frozen, dedupe first-write-wins with no conflict error (D10, AC-8). DealDossier gains no field — author rides on ReceiptEntry — and the still-stubbed renderer carries the binding forward constraint from §2.5 in its module doc (D11, AC-9). Standing gate met: grep -rin "transcript|recording" over both packages returns ZERO hits, comments and fixture filenames included. Verified: npx tsc -p packages/offer-extraction --noEmit and npx tsc -p packages/receipt --noEmit both clean; npx vitest run packages/offer-extraction packages/receipt → 90 passed / 3 files. ONE RESOLVED DEVIATION, not blocking: the builder brief says the tester owns tests, but both tsconfigs include test/ and the existing suites referenced the deleted kinds and the renamed fixture group, so leaving them untouched would have left both packages failing the AC-12 typecheck. I did a MECHANICAL migration only — transcript()→note()/callMeta() helpers, author added to every fixture, the deleted-kind cases replaced by their note/call_meta equivalents, PII fixture strings re-pointed at note text and call_meta.party — and added NO new coverage: every test in design §6 (channel-swap invariance, note-is-not-the-fallback, unknown-channel totality, the source-text transcript scan, author-mandatory and coherence type-level assertions, call-metadata purity, kinds-removed, dossier author mix) is still unwritten and remains the tester's. Two incidental notes: TypeScript rejects the coherence comparisons against the narrowed union members as provably impossible (which is D6/D7 working), so the runtime guards compare through widened MessageAuthor/MessageDirection/ReceiptEntryKind locals; and normalize.ts cites the specs/01 consent section by date rather than by its exact heading so the zero-hit grep gate stays clean. No spine change, no dependency edit, no package.json edit, no protected path touched, no file outside packages/offer-extraction/** and packages/receipt/** and this task file. stage build → test
undefined · tester · wrote the design's §6 test plan items the builder left unwritten, in two new files inside ownership: packages/offer-extraction/test/channel-agnostic.test.ts (31 tests) and packages/receipt/test/authorship.test.ts (33 tests). 154 tests across both packages, 153 pass, 1 FAILS on a real defect; both packages still typecheck clean (npx tsc -p <pkg> --noEmit, exit 0 each) — and the typecheck is load-bearing here, because every @ts-expect-error and expectTypeOf assertion is only enforced by tsc. I verified the type-level layer actually bites by mutating it (widening ReceiptEntryKind with a fifth literal, and deleting one @ts-expect-error): both mutations produced tsc errors, then I restored the file — so the compile-time half of AC-5/AC-6 is proven, not assumed. COVERED (extraction): channel-swap invariance over all four v0.5 channels on channel-NEUTRAL digit-form texts including the empty string; note treated identically to sms/email/call for every extractable field and for the full six-key offer shape; the regression the design measured — three spoken-form cases that parse under `note` and must NOT parse under sms/call, which fails loudly the moment note drifts back onto the plain fallback; PROFILE_BY_CHANNEL exhaustive over @core MESSAGE_CHANNELS, frozen, note the ONLY spoken channel and call==sms==plain (D2, so no call-text path returns in spirit); unknown/prototype-shaped ('toString','__proto__','constructor')/missing/non-string channel degrades to plain and never to a fabricated channel (D3); normalizeText total over all three profiles; ExtractionInput is exactly {channel,text} so the library has no account or deal input to cross; public surface is exactly extractOffer with no channel-named export and no exported profile; no src/rules module references a channel; a recursive scan of every .ts/.json/.md in the package for ten transcript/recording/transcription/ASR-vendor tokens (assembled from fragments so the scanner cannot self-trip); fixture groups are exactly email|hostile|notes|sms and each of the three notes/ fixtures that extracts still carries a non-digit number form — the AC-3 coverage that a quiet "twenty nine grand"→"$29,000" rewrite would have deleted while still passing the corpus; ADR-005 absent-stays-absent across all four channels (no sale_price:0, no undefined-valued key, no "sale_price":0 in the serialization) and flags always []. COVERED (receipt): author required at the type level on all four inputs plus two @ts-expect-error omission cases; ten authorless runtime shapes (undefined/null/''/'   '/'operator'/'system'/'agent'/'DEALER'/42/{}) rejected invalid_input non-retryable with read confirming NOTHING written and specifically never defaulted to 'dealer'; all three ratified labels stored verbatim; four incoherent combinations proven compile errors AND eight rejected at runtime with nothing written, against eight coherent combinations that all append (so the guard is not just refusing everything); rejection messages name the kind but leak no party/phone/body; ReceiptEntryKind is the closed four-value set with both deleted kinds unrepresentable at compile time and rejected at runtime; kind→channel injective; call-metadata purity proven structurally (keyof CallMeta and keyof CallMetaEntryInput pinned, no body) and behaviourally — a JS caller smuggling an audio URL and verbatim call text inside call_meta has both DROPPED, with the serialized entry containing neither token; nested call_meta deep-frozen; append-only proven three ways (Exclude<keyof ReceiptStore,'append'|'read'> is never, a source scan for mutation-shaped declarations, and an exhaustive "every affordance a caller has" attempt to alter or remove a stored entry) plus monotonic growth over 25 appends with 20 redeliveries and a rejected-append-leaves-history-byte-identical check; deal isolation across store AND dossier reads with near-miss bundle ids ('bundle-1 ',' bundle-1','BUNDLE-1','bundle-10') reading empty rather than falling back to a neighbour; entries carry no dealership/contact/account/salesperson key, so the tenancy split stays outside the trail; AC-9 dossier author carry-through with a dealer/buyer/concierge mixed bundle keeping all three labels distinct through JSON serialization, the concierge note never exportable as a dealer statement, and both renderers still loud not_implemented stubs. ONE FAILING TEST, one finding, implementation not touched (tester fixes tests only): packages/receipt/src/memory-store.ts validates every other caller-supplied string on the write path for blankness (receipt_bundle_id, and body per kind) but does NOT validate dedupe_key, so a blank or whitespace-only dedupe_key is accepted as a real idempotency anchor. Two UNRELATED entries carrying dedupe_key '' collide in bundle.byDedupeKey, and the second is absorbed with {ok:true, deduplicated:true} — an entry that should have entered the append-only trail is silently dropped with no error and no way for the caller to notice. D10's first-write-wins is scoped to a REDELIVERY of the same event; this is not one. The realistic trigger is precisely the JS/JSON boundary the design says the runtime guards exist for: an event envelope whose idempotency_key is absent and lands as ''. Design §4.6 rule 3 states the property this violates outright ("never silently dropped — dead-lettered comms events are the trail's feedstock, and losing one falsifies the trail"). Repro: npx vitest run packages/receipt/test/authorship.test.ts -t "blank dedupe_key". The test is deliberately REMEDY-AGNOSTIC — it accepts either fix (reject the blank key as invalid_input like every other blank string on the path, or treat it as absent and append) and fails only on the silent swallow, so the builder is not boxed into one shape. THREE RESOLVED DEVIATIONS, none blocking: (1) two of my own initial expectations were wrong, not the implementation — the fee name is verbatim-cased from source text ('doc fee', not 'Doc fee'), and the design §1.5 reviewer note ("every new notes/ file still contains a non-digit number form except note-scheduling-no-offer") over-counts, since note-vague-no-numbers is specified in that same table as having no figures in it; I re-pointed the assertion at the load-bearing invariant instead (every notes/ fixture that EXTRACTS must keep a spoken form), which is stricter where it matters and correct where the design table is not. (2) ADR-008's skip-when-config-absent rule gates nothing in T-012: both packages are pure, and no test in either file reaches a database, an object store, or an HTTP endpoint, so there is no DB- or endpoint-dependent case to skip — nothing is reported skipped and nothing is faked as passed. Stated in both file headers so a later reader does not mistake the absence of skips for an omission. (3) Kit mandates outside these two packages are not testable here and are correctly absent rather than stubbed: write-once target_vehicle and make/model mismatch rejection have no surface in either package (the extractor has no vehicle, valuation, or deal input at all — asserted via keyof ExtractionInput), and ADR-007's retail-band comparison belongs to the flag engine (T-011). I ran nothing against packages/flag-engine, packages/adapters/*, or services/comms and report no breakage there. No implementation file, no spine file, no dependency, no package.json, no protected path touched; writes confined to the two new test files and this task file. stage test → validate
