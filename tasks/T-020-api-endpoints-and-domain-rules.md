---
id: T-020
title: Deal / thread / message / offer / dealership endpoints — write-once target, mismatch rejection
stage: validate
owner_agent: tester
status: in_progress
depends_on: [T-011, T-019]
file_ownership:
  - "services/api/src/routes/**"
  - "services/api/test/routes/**"
spec_refs:
  - "specs/00-shared-core-architecture.md#core-domain-model"
  - "specs/00-shared-core-architecture.md#cardinality-invariants-structurally-enforced"
  - "specs/00-shared-core-architecture.md#budget-ceiling-vs-fair-price--two-different-questions-resolved-2026-08-07"
  - "specs/00-shared-core-architecture.md#dealership-data-tenancy"
  - "specs/00-shared-core-architecture.md#flag-engine-shared"
  - "specs/00-shared-core-architecture.md#comms-aggregation-layer-provider-agnostic--shared"
  - "specs/01-consumer-product-spec.md#web-surface-war-room-first"
  - "decisions/OPEN-QUESTIONS.md (Q11 amended, Q12 amended, Q16, Q20, Q22)"
  - "decisions/adr/ADR-005-offer-sale-price-optional.md"
  - "decisions/adr/ADR-006-current-offer-rollup-policy.md"
mock_only: false
---

## Objective

The API's resource surface: deals, dealer threads, vehicle instances, messages, offers, dealerships, and dealership contacts, over the v0.5 spine and through T-019's account context, validation, and error envelope. Two spec rules are enforced here as API behaviour: `target_vehicle` is **write-once** (settable while the deal is `draft`, rejected once any offer is attached), and a `VehicleInstance` whose make/model does not match the deal anchor is **rejected**, identified by its VIN, offered a new-deal path, and the rejection is written to the receipt trail. Year drift is not a rejection, and VIN stays user-entered and unvalidated.

## Acceptance criteria

1. Endpoints exist for deal, dealer-thread, vehicle-instance, message, offer, dealership, and dealership-contact operations over the v0.5 spine types — cites specs/00-shared-core-architecture.md "Core domain model".
2. `target_vehicle` (make, model, optional `year_range`) is settable while the deal is `draft`; once any offer is attached to the deal, a write to make or model is rejected — cites specs/00-shared-core-architecture.md "Cardinality invariants (structurally enforced)" ("`target_vehicle.make`/`model` are write-once — settable while the deal is `draft`, immutable once any offer is attached") and decisions/OPEN-QUESTIONS.md Q11 (AMENDED).
3. Submitting a `VehicleInstance` whose make/model does not match the deal's anchor is rejected, the response identifies the offending vehicle by its VIN so the client can highlight it, and the response offers opening a new deal — cites specs/00-shared-core-architecture.md "Cardinality invariants" ("the app **rejects the entry, highlights that vehicle in red against its VIN, and offers to open a new Deal**") and specs/01-consumer-product-spec.md "Web surface (war-room-first)" W2 ("A vehicle entered against the wrong make/model is **rejected and shown in red against its VIN**, with a prompt to open a new deal").
4. Every such rejection is recorded in the deal's receipt trail as an event, so a dealership's substitution attempt leaves a mark rather than vanishing — cites specs/00-shared-core-architecture.md "Cardinality invariants" ("The rejection is a receipt-trail event") and decisions/OPEN-QUESTIONS.md Q11.
5. A vehicle instance whose year falls outside the deal's `year_range` is accepted — year drift is a soft guide, never a rejection — cites specs/00-shared-core-architecture.md "Cardinality invariants" (Year drift: "a **soft guide, not a hard rejection**") and decisions/OPEN-QUESTIONS.md Q16.
6. VIN is accepted as user-entered and unvalidated; no endpoint gates acceptance on a decode lookup — cites decisions/OPEN-QUESTIONS.md Q16 ("VIN is **user-entered and unvalidated** at launch — the buyer's own record, not a lookup key").
7. Message creation accepts `channel: note` with `direction: internal` and a required `author`, and note bodies are run through offer extraction like any other message — cites specs/00-shared-core-architecture.md "Core domain model" (`Message` block) and specs/00 "Comms aggregation layer" ("any message text — buyer note, SMS, or email — → parsed `Offer`"), plus decisions/OPEN-QUESTIONS.md Q22.
8. `author` is always supplied by the caller and never inferred by the API — cites specs/00-shared-core-architecture.md "Core domain model" (`author ... who produced this text — never inferred`).
9. Thread endpoints expose and allow updating `dealership_id`, `vehicle_instance`, `working_with`, and `process_step` across the six-step sequence — cites specs/00-shared-core-architecture.md "Core domain model" (`DealerThread` block), decisions/OPEN-QUESTIONS.md Q12, and specs/01-consumer-product-spec.md "Web surface (war-room-first)" W2 ("per-dealership **who you're working with** ... plus **process step**").
10. Dealership create/lookup writes and reads the GLOBAL record; contacts are written to and read from the account-private store, and no endpoint can return another account's contact — cites specs/00-shared-core-architecture.md "Dealership data tenancy" and decisions/OPEN-QUESTIONS.md Q12 (AMENDED).
11. Offer representations carry `flags[]` over the v0.5 five-flag set including `above_market`, alongside the set of flags that could not be evaluated — cites specs/00-shared-core-architecture.md "Flag engine (shared)" (five-flag list) and decisions/adr/ADR-005-offer-sale-price-optional.md "Decision" §2.
12. An offer on a thread whose vehicle instance has no valuation is represented as fair-price **unevaluable**, never as passing — cites specs/00-shared-core-architecture.md "Budget ceiling vs. fair price — two different questions" ("a thread with no valuation yet reports fair-price as **unevaluable** ... never as 'fine'") and decisions/OPEN-QUESTIONS.md Q20.
13. An offer with no stated sale price is representable and is not rendered as a zero price — cites decisions/adr/ADR-005-offer-sale-price-optional.md "Decision" §1.
14. A thread's `current_offer` is exposed as the accumulated per-field newest-wins rollup — cites decisions/adr/ADR-006-current-offer-rollup-policy.md "Decision".
15. `over_walkaway` (deal budget ceiling) and `above_market` (this car's own valuation) are surfaced as distinct signals, never fused into a single verdict — cites specs/00-shared-core-architecture.md "Budget ceiling vs. fair price — two different questions" (the two-question table) and specs/00 "Flag engine (shared)".
16. Every endpoint goes through T-019's account context, validation, and shared error envelope; none defines its own — cites decisions/adr/ADR-001-backend-language-node-ts.md "Decision".
17. Route tests cover the write-once rejection, the make/model mismatch rejection plus its receipt entry, the year-drift acceptance, and the unevaluable fair-price representation.

## Notes / constraints

- Owns `services/api/src/routes/**` and `services/api/test/routes/**` only. Everything else under `services/api/` belongs to T-019; this task depends on it, which is what legalises the nested ownership.
- Depends on T-011 because the flag vocabulary and the evaluated/unevaluable split it surfaces are the flag engine's output contract.
- **Cross-thread ranking is value-adjusted, not raw-price** — if any ordering or comparison is exposed by these endpoints it must rank offers against each car's own valuation (specs/00 "Budget ceiling vs. fair price", Consequences bullet 3). If the designer judges ranking to be a UI concern rather than an API one, leave it out rather than inventing a half-version.
- **Auth is E3.** Do not add roles, grants, or sessions; route handlers call through the authorization choke point T-019 declares.
- Do not touch protected paths: `CLAUDE.md`, `thesis.md`, `specs/**`, `.claude/**`.

## Log

<!-- append-only; one line per event: YYYY-MM-DD HH:MM · agent · event -->
2026-08-07 14:00 · planner · task created (Epic 2 Half B: persistence & API spine)
undefined · designer · design published (docs/design/T-020.md); stage design → build. Shape: 18 routes under services/api/src/routes/**, all registered through T-019's `buildServer({ routes })` inside the api scope, every `:deal_id` route carrying both boot-audit marks (validate + dealGate), no DELETE anywhere and no PUT/PATCH on /messages,/receipt,/notes. Load-bearing calls: (D1) a deal is created by `PUT /deals/:deal_id` with a caller-chosen id, because a `POST /deals` has no `:deal_id`, therefore no `DealHandle`, therefore NO reachable store — creation is inexpressible through T-019's only door; `owner_id` comes from the handle's scope and never from the body. (D2) every id this API mints is deterministic (`receipt:<deal_id>`, `vi:<deal_id>:<dealership_id>`, `dl-<slug>-<fnv1a32>` over migration 0004's natural key), so replays are byte-identical and no `node:crypto` import is needed (T-019's import allowlist covers src/routes/**). (D3) a vehicle-instance submission carries make/model at the WIRE ONLY — compared to the anchor, then discarded, because `@core.VehicleInstance` has no such field, which is exactly why the check belongs at the API boundary; mismatch → 422 + receipt event, matching case stores the spine shape unchanged. (D5) write-once rejection is 409 (the shipped `ApiErrorCode` comment) and mismatch is 422. (D6) the rejection response CANNOT echo the VIN — `toEnvelope` strips `details` for every code except `invalid_request` — so the VIN's durable home is the receipt entry and the client highlights the vehicle it just submitted; §8.3 offers the lead the ADR-sized alternative. (D8) the rejection receipt is appended in its OWN unit of work after the mutating session is rolled back, exactly as migration 0014 requires ("a SEPARATE insert outside the aborted transaction"). (D9) `unevaluable` is derived in src/routes/ from input presence and pinned against `@flag-engine` by a parity test in test/routes/** — so the production path stays verdict-free (T-019 D10 holds) while drift becomes a test failure. (D10) three per-flag states — flagged | unevaluable | not_evaluated — and NEVER "clear": `rollup.ts` and `extract.ts` both hard-code `flags: []`, so reading an empty list as "fine" would report every offer as fine, which specs/00 forbids outright. (D11) `over_walkaway` and `above_market` ship in two separate objects with separate reasons and NO ranking is exposed (value-adjusted ordering is unbuildable while every fair-price answer is unevaluable). (D13) note `author` is required from the caller AND checked against the session role (403 on a claim the session cannot back), satisfying both AC-8 and @comms's "never from a request body". (D15) the global `Dealership` is create-or-find only — no update path, because that table is writable by every account. (D16) no second health endpoint; /healthz + /readyz are T-019's. ONE ITEM NEEDS THE LEAD BEFORE BUILD CAN LAND GREEN: services/api/test/static-invariants.test.ts (T-019's file) asserts `src/routes/**` and `test/routes/**` do NOT exist — it goes red the moment this task does its job, and T-020's builder must not edit it; §8.1 names the exact one-case amendment and confirms every other src/** scan in that suite is satisfied by this design. Non-blocking notes to lead: (§8.2) E3's real gate must permit `write` on an unknown deal id or T-019 must gain a `create` action, else deal creation breaks; (§8.3) T-019's §5.6 prose says 422 for write-once while its code says 409 — one-line doc correction; (§8.4) no durable home exists for the GLOBAL dealership record (@comms has no method, @store-pg D15 never writes `dealerships`), so this design declares a `DealershipDirectory` port with an in-memory impl and recommends a later task give it a Postgres implementation and reconcile the slug id with 0004's uuid PK; (§8.5) `@receipt` has no `system` author/kind, so rejection marks are account-authored notes with a `[system]` body prefix; (§8.6) nothing in this epic runs the flag engine, which is why `not_evaluated` exists; (§8.7) HIGHEST-VALUE FOLLOW-UP — `putThread`/`putDeal` are seed-or-replace, so every `process_step` or `working_with` update rebuilds the thread record, losing the store's `message_ref` correlation index and resetting ADR-006's `RollupState`; no route can avoid it (there is no partial-update method) and `@comms` is not this task's to edit, so the build should PIN the behaviour with a test and the lead should schedule a `@comms`-owned in-place update; (§8.8) deliberate absences recorded — no `GET /deals` account list (no port exposes one without an unscoped scan), no `POST /offers` (ADR-006's rollup is the only producer), no dealership update/delete, no cross-thread ranking, no outbound send, and `rate_markup` is always unevaluable because no prequal source is reachable in E2. No spine change, no integration, no dependency or manifest edit, no protected path touched. Not blocked.
undefined · builder · build complete (services/api/src/routes/**, 16 modules, 18 routes); stage build → test. `npx tsc -p services/api --noEmit` clean; the whole `services/api` suite runs 158 passed / 4 skipped with ONE failure, and it is the one §8.1 predicted: T-019's `services/api/test/static-invariants.test.ts` asserts `src/routes/**` does not exist, which this task's existence falsifies. That file is T-019's and was not edited — the lead must assign the one-case amendment. Every OTHER static scan in that suite is green against this build: import allowlist (fastify/zod/@core/@comms/@db/@object-store/@receipt only — no `@flag-engine`, no `@adapters/*`, no `@offer-extraction`, no `node:crypto`, no `node:process`), one `DealHandle` mint site, one identity reader, one `process.env` reader, no unscoped operator surface, no auth-provider name, no removed vocabulary. Implemented per design: D1 (`PUT /deals/:deal_id` creates — a `POST` has no `:deal_id`, hence no handle, hence no reachable store), D2 (every minted id deterministic: `receipt:<deal_id>`, `vi:<deal_id>:<dealership_id>`, `dl-<slug>-<fnv1a32>`), D3/D4 (make+model carried at the wire only, compared `trim().toLowerCase()`, then discarded), D5 (write-once → 409, mismatch → 422), D6 (no VIN on the wire; its durable home is the receipt entry), D7/D8 (`note`/`internal` mark with a `[system]` prefix, appended in its OWN unit of work after the mutating session is rolled back with nothing written; a failed mark never changes the caller's outcome), D9 (`deriveEvaluability` from input presence — the production path stays verdict-free), D10 (three states, never a "clear"), D11 (`budget` and `fair_price` as separate objects; no ordering, no ranking), D12 (`dealership_id` set at create, never mutated), D13 (`author` required AND checked against the session role → 403), D14 (no `POST /offers`), D15 (dealership create-or-find only), D16 (no second health endpoint). FOUR DEVIATIONS, all resolved in place and all recorded in code comments: (1) design §4.2 wanted a "locked-by" label on the `target_vehicle_write_once_reject` log line — DROPPED, because naming which disjunct fired restates part of `isTargetVehicleLocked` and T-019's `test/cardinality.test.ts` ("has no rival implementation in this service") correctly refuses that; one predicate, one place. (2) `DealershipDirectory.ensure` returns a three-member union (`created` | `found` | `id_collision`) instead of `{dealership, created}` — the declared shape cannot express the `dealership_id_collision` 409 the same design's §4.2 error table requires, and house style is failures-as-values. (3) one module beyond §1.1's inventory, `src/routes/context.ts`, holds the shared route runtime and `runGuarded`; the three alternative homes were all wrong (`aggregate.ts` is pure, `views.ts` is types, `deals.ts` would make every route file import a sibling route module) and `index.ts` re-exports §2.1's surface unchanged. (4) `ThreadAssessmentView.dealership` is optional rather than required — the API never fabricates a global dealership row when the directory has none. NOTES FOR THE LEAD, unchanged from design §8: §8.2 (E3's gate must permit `write` on an unknown deal id, or T-019 needs a `create` action), §8.4 (no durable home for the global dealership record yet), §8.6 (nothing in this epic runs the flag engine — the reason `not_evaluated` exists), §8.7 (`putThread`/`putDeal` are seed-or-replace, so a `process_step` write rebuilds the thread record and drops the store's `message_ref` index and `RollupState`; unavoidable from a route, needs a `@comms`-owned in-place update, and the tester should PIN the behaviour). No spine change, no integration, no dependency or manifest edit, no protected path touched, no foreign file edited. Not blocked.
undefined · tester · tests written and green (services/api/test/routes/**, 15 files); stage test → validate. `npx vitest run services/api/test/routes` = 168 passed / 2 skipped / 0 failed; `npx tsc -p services/api --noEmit` clean. The 2 skipped are `routes-storage.db.test.ts`, which is `describe.skipIf(DATABASE_URL unset)` per ADR-008 — reported as SKIPPED, never as passed: with no database configured this suite proves the route contract over the in-memory DEFAULT posture only, and nothing in it lets a green run claim the Postgres path works. Coverage by acceptance criterion and kit mandate: AC-2/AC-4 write-once (`target-vehicle-write-once.test.ts`, 12) — settable in draft, 409 once a deal offer attaches, once a THREAD `current_offer` exists, and once status leaves draft, each rejection asserted three ways (status/code + stored aggregate unchanged + exact trail delta), plus a state-matrix case that pins the route's lock decision to `@core.isTargetVehicleLocked` so a rival predicate cannot appear; AC-3/AC-4 mismatch (`vehicle-instance-mismatch.test.ts`, 14) — 422 on make and on model, nothing stored, an already-stored instance never replaced, the VIN ABSENT from the wire (and `details` absent from a 422) while present in the receipt entry with `submitted=`/`anchor=`, two attempts leaving two marks and a `client_attempt_ref` retry collapsing to one; AC-5/AC-6 (`year-drift-and-vin.test.ts`, 9) — nine years from 1900 to 2200 all 200 with no 4xx anywhere, boundary years emit no advisory, widening the guide clears the advisory without touching the car, any non-blank VIN accepted including 1- and 64-char and non-VIN strings, and a valuation SPY proves acceptance consulted no lookup at all; AC-7/AC-8 (`messages.test.ts`, 16) — note/internal fixed by the service and unstatable by the caller, the extraction event asserted on the queue after drain, replay → `duplicate`, author absent → 400 (never defaulted), `dealer` unrepresentable, and BOTH role mappings checked (owner⇒buyer, concierge_agent⇒concierge, each refusing the other with 403); AC-9 (`threads.test.ts`, 16) — all six `PROCESS_STEPS`, `working_with` round-trip, and D12 asserted the way the design argues it (no shape expresses a `dealership_id` change, and the same request at another URL creates a SECOND thread while the first keeps its messages); AC-10 (`dealerships-tenancy.test.ts`, 12) — two accounts converge on one global row, the messy natural key normalizes to the same deterministic id, no update/delete path, five public fields only, no contact route exists at any global URL, and two accounts sharing one dealership never see each other's `working_with`; AC-11..AC-15 (`offers-assessment.test.ts`, 21) — all five flags in `OFFER_FLAGS` order, no `clear`/`passing`/`fair`/`fine` state emitted anywhere, `sale_price` absent stays an OMITTED key (never 0, never null), `above_market` unevaluable with the reason naming the missing input, and ADR-007 pinned three ways: a snapshot carrying wholesale+trade_in+private_party but NO retail is `no_retail_band` (no fallback band), a foreign car's snapshot is never reachable, and a matching retail band yields `not_evaluated` rather than "fine"; ADR-006 `current_offer` byte-identical to the aggregate's and no POST that could mint a second one. Plus `evaluability-parity.test.ts` (D9) — 384 input combinations × 3 `FlagEngineConfig` values asserting `deriveEvaluability` equals `evaluateOffer(...).unevaluable`, and that the answer is config-invariant, so the verdict-free production path cannot silently drift from the engine; `audit-compliance.test.ts` — the real suite boots with an empty audit table, its exact 18-route inventory is asserted, every one of the 15 `:deal_id` routes carries both marks, and the boot REFUSAL still fires for a gate-less route and for a DELETE registered alongside this suite; `error-paths.test.ts` — the rows nothing else reaches: dealership id_collision → 409 without echoing the row, `recorded_extraction_deferred` → retryable 503 with the note already durable and the same `client_note_ref` retry absorbed, a rejection on a deal with no `receipt_bundle_id` still rejecting rather than 500ing, and `appendRejectionMark` proven not to change the caller's outcome when the store fails as a value AND when it throws; `static-invariants.test.ts` (mine, scanning `src/routes/**` only) — no `@flag-engine`/`@adapters/*`/`@offer-extraction`, no `node:` builtin, no `Math.random`, no `as DealHandle`, no `req.headers`, no parallel spine type, no rank/score/verdict key, no audio or transcription vocabulary, no credit-payload field, and no provider name or network primitive, so the mock-only integrations stay unreachable by construction. `seed-or-replace.test.ts` PINS design §8.7 as the design asked: a deal-level PATCH drops the store's `message_ref` index so a later `attachExtractedOffer` returns `not_found`, and a thread step change resets ADR-006's provenance so an OLDER contribution overwrites a newer field — both verified against actual behaviour, with a control case showing newest-wins holds when no state write intervenes; when the `@comms`-owned in-place update lands these fail loudly and become the fix's checklist. ONE PRE-EXISTING FAILURE, NOT MINE AND NOT IN MY PATHS: `services/api/test/static-invariants.test.ts` (T-019's file) asserts `src/routes/**` and `test/routes/**` do NOT exist. It was already red before this task's tester wrote a line — design §8.1 predicted it and the builder's log escalated it — and creating `test/routes/**` now falsifies its third assertion too, so the lead's one-case amendment must cover both. I did not edit that file. Whole-service run for context: 325 passed / 1 failed (that case) / 6 skipped. No implementation file was touched, no spine change, no protected path, no dependency or manifest edit. Not blocked.
