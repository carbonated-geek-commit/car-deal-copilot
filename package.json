{
  "name": "deal-copilot-kit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/adapters/*",
    "services/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p packages/core --noEmit && tsc -p packages/flag-engine --noEmit && tsc -p packages/offer-extraction --noEmit && tsc -p packages/receipt --noEmit && tsc -p packages/adapters/valuation --noEmit && tsc -p packages/adapters/nhtsa --noEmit && tsc -p packages/adapters/vehicle-history --noEmit && tsc -p packages/adapters/credit-prequal --noEmit && tsc -p services/comms --noEmit && npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "vitest": "^3"
  }
}
